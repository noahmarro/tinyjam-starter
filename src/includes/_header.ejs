<!DOCTYPE html>
<html>
  <head>
    <title><%= root.data.title %></title>
    <meta charset="utf-8">
    <link rel="alternate" type="application/rss+xml" href="rss.xml" title="<%= rootPath %>/<%= root.data.title %> RSS">
    <style>
      body {
        background: hsl(0 0% 10%);
        color: hsl(0 0% 90%);
        font: 16px/1.5 sans-serif;
      }
      .container {
        max-width: 600px;
        margin: 40px auto;
      }
      img.logo {
        float: right;
      }
      a {
        color: hsl(24 76% 61%) ;
      }
      hr {
        border: 0;
        height: 1px;
        background: hsl(0 0% 70%);
        margin-top: 30px;
      }
    </style>
  </head>
  <body>
    <div class="container">
